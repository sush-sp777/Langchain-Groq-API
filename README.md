# LangChain Groq Language Translation API 🚀

This project builds a simple **translation API** using:

- LangChain  
- Groq 'llama-3.1-8b-instant' model
- FastAPI  
- LangServe  

It translates text into any language by calling a Groq LLM through a LangChain runnable wrapped inside FastAPI.

---

## ✨ Features

- Uses Groq’s ultra-fast **llama-3.1-8b-instant** model  
- FastAPI-based backend  
- LangServe auto-generated LLM routes  
- Clean & modular chain  
- Easy to run locally or deploy  

---

## 🔧 Installation

### 1. Clone the Repository

```bash
git clone https://github.com/sush-sp777/Langchain-Groq-API.git
cd Langchain-Groq-API
```

### 2. Install Requirements

```
pip install -r requirements.txt
```

---

## 🔑 Environment Variables

Create a `.env` file in the project folder and add:

```
GROQ_API_KEY=your_groq_key_here
```


## ▶️ Run the Server

Start FastAPI using:

```
python serve.py
```

API will run at:

```
http://127.0.0.1:8000
```

---

## 🌐 API Endpoints (Auto-Generated by LangServe)

- **POST /chain/invoke** → Invoke the chain once  
- **POST /chain/stream** → Stream responses token-by-token  
- **POST /chain/batch** → Send multiple inputs  
- **GET /chain/input_schema** → View input fields  
- **GET /chain/output_schema** → View output format  
- **GET /docs** → Swagger UI  
- **GET /redoc** → API docs  

---

## 🧪 Example Request

POST to:

```
http://127.0.0.1:8000/chain/invoke
```

Request Body:

```
{
  "input": {
    "language": "marathi",
    "text": "How are you?"
  }
}
```

---

## 📘 Summary

This project teaches how to:

- Build a LangChain runnable  
- Connect Groq LLaMA models  
- Wrap it in FastAPI  
- Use LangServe to auto-create all useful routes  


---


